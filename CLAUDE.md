## Agent skills

### Issue tracker

Issues are tracked in GitHub using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Using standard triage labels (e.g., `needs-triage`, `ready-for-agent`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout with `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
